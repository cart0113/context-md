---
description:
  Instructions the agent must read once per session at startup. Orienting
  content — project identity, hard invariants, workflow entry points.
  Substantive is OK; this only fires once per session.
---

<!--
  This file is inlined RAW at session startup. Frontmatter above is
  stripped; everything below is what the agent sees.

  You own the framing. The tool adds no heading, no preamble, no path
  attribution. Write the heading you want the agent to see.

  Reasonable length: 10–40 lines. Keep it orienting, not exhaustive —
  deeper material belongs in context-db files the agent fetches via the
  TOC when relevant.
-->

# On Start

<replace this with project orientation the agent needs before doing work>
