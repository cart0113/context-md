---
description:
  Instructions the agent must read on every /context-db subcommand. Keep
  EXTREMELY brief — this fires in front of every prompt, pre-review, review,
  update, and maintain call. Every line burns context every time.
---

<!--
  This file is inlined RAW on every /context-db subcommand. Frontmatter
  above is stripped; everything below is what the agent sees.

  You own the framing. The tool adds no heading, no IMPORTANT! line, no
  file-path attribution. If you want a heading or emphasis, write it here.

  Keep it to 2–5 short lines. Good candidates:
    - a non-obvious invariant agents keep violating
    - a one-line pointer to a dangerous gotcha
    - a naming or structural rule easy to break accidentally

  Orientation content and anything the agent only needs once belongs in
  ON_START.md or a regular context-db file — not here.
-->

# On All

<replace this with your brief, always-on rules>
