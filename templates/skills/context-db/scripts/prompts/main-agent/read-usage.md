# Read Usage

If you need the full content of any file or folder inlined (e.g. to read an
entire standards folder without hopping through the TOC), you can run:

    /context-db read <path-or-glob> [<path-or-glob> ...]

Folders expand recursively. Globs are resolved relative to cwd.
